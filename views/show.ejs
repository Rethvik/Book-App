<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
    <title><%=title%></title>
  </head>
  <body>
    <div style="width: 100vw; height: 90vh;padding-right: 40px;"  class="position-relative">
    <% if(found.length===0) {%>
      <h1 style="text-align:center;">No users</h1>  
      <a href="/create">Create User</a>
      <% }else{ %> 
    <h1 style="text-align: center;">All Users</h1>
    <% for(let user of found ){ %>
    <div class="list-group">
      <ul>
        <li class="list-group-item">User Name: <strong><%=user.Name%></strong></li>
        <li class="list-group-item">Earnings: <strong><%=user.TotalEarnings%></strong></li>
        <% if(user.ReferedUser === null) {%>
        <li class="list-group-item">Refered User: <strong>---</strong></li>
        <%}else{ %>
        <li class="list-group-item">Refered User: <strong><%=user.ReferedUser%></strong></li>
        <% } %> <%if (user.isPaymentMade){%>
        <li class="list-group-item"><strong>Payment is already updated</strong></li>
        <%}else {%>
        <li class="list-group-item"><strong>
          Payment is not updated ||Click on the update payment button to update
        </li></strong>
        <li class="list-group-item"><form action="/pay-update/<%=user._id%>" method="post">
          <button class="btn btn-primary">Update Payment</button>
        </form>
        </li>
        <%}%>
      </ul>
    </div>
    <% } %>
    <% } %> 
    <a class="btn btn-info position-absolute bottom-0 start-50 translate-middle-x" role="button" href="/">Home</a>
    </div>
  </body>
</html>
